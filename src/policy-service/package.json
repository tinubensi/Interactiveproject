{
  "name": "policy-service",
  "version": "1.0.0",
  "description": "Policy Service - Manages policy issuance and lifecycle",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "func start --port 7074",
    "prestart": "npm run build",
    "clean": "rimraf dist",
    "setup": "npm run build && node dist/scripts/setup-database.js",
    "test:api": "bash scripts/test-api.sh"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/eventgrid": "^5.0.0",
    "@azure/functions": "^4.0.0",
    "@azure/storage-blob": "^12.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}

