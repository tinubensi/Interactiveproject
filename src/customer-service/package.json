{
  "name": "customer-service",
  "version": "1.0.0",
  "description": "Customer Service Azure Functions",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "npm run build && npx func start --port 7076",
    "start:alt": "npm run build && func start --port 7073",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/eventgrid": "^4.0.0",
    "@azure/functions": "^4.0.0",
    "@azure/identity": "^4.0.0",
    "@types/nodemailer": "^7.0.4",
    "nodemailer": "^7.0.10",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "@types/uuid": "^9.0.0",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
