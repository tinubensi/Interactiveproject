{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "npx func start --port 7071",
    "test": "npm run build && node --test \"dist/src/tests/**/*.js\"",
    "test:watch": "tsx watch --test \"src/tests/**/*.test.ts\"",
    "test:coverage": "npm run build && node --test --experimental-test-coverage \"dist/src/tests/**/*.js\"",
    "precommit": "npm test",
    "verify": "npm run test:coverage",
    "migrate:connectors": "tsx scripts/migrateConnectorConfigs.ts",
    "migrate:connectors:live": "tsx scripts/migrateConnectorConfigs.ts --live"
  },
  "dependencies": {
    "@azure/cosmos": "^4.8.0",
    "@azure/functions": "^4.0.0",
    "@nectaria/auth-middleware": "file:../../../packages/@nectaria/auth-middleware",
    "@nectaria/shared-types": "file:../../../packages/@nectaria/shared-types",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "jsonata": "^2.1.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "husky": "^9.0.0",
    "rimraf": "^5.0.0",
    "tsx": "^4.7.0",
    "typescript": "^4.0.0"
  }
}
